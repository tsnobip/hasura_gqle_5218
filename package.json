{
  "name": "hasura",
  "version": "0.1.0",
  "description": "packages for hasura",
  "license": "MIT",
  "devDependencies": {
    "hasura-cli": "^1.3.0-beta.2"
  },
  "scripts": {
    "start": "cd local; docker-compose up -d",
    "stop": "cd local; docker-compose down",
    "console": "hasura console --admin-secret=myadminsecretkey"
  }
}